# Black Metal theme by metalelf0
background  #000000
foreground  #ffffff
cursor      #ffffff
selection_foreground #000000
selection_background #ffffff
color0   #000000
color1   #5f8787
color2   #dd9999
color3   #a06666
color4   #888888
color5   #999999
color6   #aaaaaa
color7   #c1c1c1
color8   #333333
color9   #5f8787
color10  #dd9999
color11  #a06666
color12  #888888
color13  #999999
color14  #aaaaaa
color15  #c1c1c1

# Quickshell can override above when it generates a theme
include ~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf

# Font
font_family      JetBrains Mono Nerd Font
font_size 11.0

# Hide tab bar (tmux handles tabs)
tab_bar_style hidden

# Cursor
cursor_shape beam
cursor_trail 1

# Padding (why weird value? consistency with foot)
window_margin_width 21.75

# No stupid close confirmation
confirm_os_window_close 0

# Launch tmux (attach to existing session or create new)
shell zsh -c "tmux new-session -A -s main"

# Open file paths from terminal output (Ctrl+Shift+E)
map ctrl+shift+e kitten hints --type path --program -

# Open file paths in editor (Ctrl+Shift+O)
map ctrl+shift+o kitten hints --type path

# Clickable URLs
mouse_map ctrl+left press ungrabbed,grabbed mouse_click_url

# Copy
map ctrl+c    copy_or_interrupt

# Search
map ctrl+f   launch --location=hsplit --allow-remote-control kitty +kitten search.py @active-kitty-window-id
map kitty_mod+f   launch --location=hsplit --allow-remote-control kitty +kitten search.py @active-kitty-window-id

# Scroll & Zoom
map page_up    scroll_page_up
map page_down    scroll_page_down

map ctrl+plus  change_font_size all +1
map ctrl+equal  change_font_size all +1
map ctrl+kp_add  change_font_size all +1
map ctrl+minus       change_font_size all -1
map ctrl+underscore       change_font_size all -1
map ctrl+kp_subtract       change_font_size all -1
map ctrl+0 change_font_size all 0
map ctrl+kp_0 change_font_size all 0
